{% extends 'base.html.twig' %}

{% block title %}Edit Annonce{% endblock %}

{% block body %}
    <h1 style="font-family: 'Bebas Neue';  font-size: 66px; color:white; text-shadow: 1px 1px 2px black;"><center>Edit Annonce<center></h1>
    <a class="btn btn-dark btn-block" href="{{ path('admin_annonce_index') }}"><i class="fas fa-undo"></i></a>
    <br>
    {{ include('admin/annonce/_form.html.twig', {'button_label': 'Update'}) }}

    {{ include('admin/annonce/_delete_form.html.twig') }}
{% endblock %}
