
<br>
<form method="post" action="{{ path('admin_annonce_delete', {'id': annonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ annonce.id) }}">
    <button class="btn btn-danger btn-sm btn-block"><i class="far fa-trash-alt"></i></button>
</form>
